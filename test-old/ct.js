	/**
	 * 一系列依赖语法树的类所组成的包
	 * @param {SyntaxTree} syntaxTree - 依赖的语法树
	 */

		/**
		 * 查询2个节点之间的其他节点，对此些节点，回调函数应将会对其做出处理
		 * @param {Node} first - 第一个节点
		 * @param {Node} first - 第一个节点
		 */