var a = 1;

1,2,3,4,5 + 6 - 11 > a ? a += 100 : a -= 100, a += 5

if(
	a !== -94
){
	throw "不正确的计算结果1"
}

a += 3 , a - 6 > 50 ? (a += 100) : (a -= 99, 1,2,3), 99

if(
	a !== -190
){
	throw "不正确的计算结果2"
}